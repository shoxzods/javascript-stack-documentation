// ---------------------- SQL Shell -------------------------------------- //;

SQL Shell - это основано не терминал и можно запустить inline команды внутри SQL терминала.

Server [localhost]: auto
Database [postgres]: auto
Port [5432]: auto
Username [postgres]: auto
Password for user postgres: own password


SELECT version(); - показывает версию postgreSql.

Всегда вставте ; semicolon а то код не запустится.